---
import HowTo from "../../components/how-to.astro";
import Layout from "../../layouts/main.layout.astro";

const profiles = [
  {
    name: "Luis Höfer",
    path: "/profiles/luis-hoefer",
    image: "/images/luishoefer.jpeg", // Add the image path for each profile
  },
  // Add more profiles here as needed
];
---

<Layout title="WeJob - Developer Profiles">
  <HowTo />
  <div class="font-medium text-xl mb-4">Profiles</div>
  <div class="flex flex-wrap -mx-2">
    {
      profiles.map((profile) => {
        return (
          <div class="w-full sm:w-1/2 md:w-1/3 px-2 mb-4">
            <div class="card bg-base-100 shadow-xl">
              {/* <figure>
                <img
                  src={profile.image}
                  alt={profile.name}
                  class="w-full h-48 object-cover"
                />
              </figure> */}
              <div class="card-body">
                <h2 class="card-title">{profile.name}</h2>
                <div class="card-actions justify-end">
                  <a href={profile.path} class="btn btn-primary">
                    View Profile
                  </a>
                </div>
              </div>
            </div>
          </div>
        );
      })
    }
  </div>
</Layout>
